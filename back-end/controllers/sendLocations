const db = require('../models/db');

const sendLocations = (req, res, next) => {
    db.query('SELECT * FROM location', (error, results) => {
        if (error) {
          console.error(error);
          res.status(500).json({ error: 'Error fetching categories' });
        } else {
          res.json(results);
        }
      });
}

module.exports = { sendLocations };